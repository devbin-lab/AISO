# Direct runtime dependencies are intentionally exact. Their transitive dependency
# versions live in requirements.lock and are supplied to pip as constraints by the
# packaged-runtime build. Update both files together after CI verification.
fastapi==0.139.0
uvicorn==0.50.2
httpx==0.28.1
discord.py==2.7.1
playwright==1.61.0
numpy==2.5.1
pypdf==6.14.2
openpyxl==3.1.5
python-docx==1.2.0
olefile==0.47
send2trash==2.1.0
